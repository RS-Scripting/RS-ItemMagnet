# Commands

The main plugin command is:

```text
/rsim
```

## `/rsim`

Displays the running RS-ItemMagnet plugin information.

## `/rsim version`

Displays the installed RS-ItemMagnet version.

## `/rsim reload`

Reloads the RS-ItemMagnet configuration.

This command requires the admin permission.
