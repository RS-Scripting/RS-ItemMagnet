# Permissions

## `rsitemagnet.admin`

Provides administrative access to RS-ItemMagnet features.

Default:

`op`

## `rsitemagnet.bypass`

Allows the player to bypass ItemMagnet ownership protection.

Default:

`op`

## OP Bypass

The `admin-bypass` configuration setting controls whether OP players receive the plugin's configured administrative bypass behavior.
